using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ConfigurationLoader : MonoBehaviour
{
    /* What this script is for is making it so when the game starts, the game will load a list of commands in the order the game is supposed to execute them.
     This script essentially gets that list and then adds the commands to the database.
     */


    // Start is called before the first frame update
    private void Awake()
    {

    }
}
